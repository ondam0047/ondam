import type { ReactNode } from "react";
import "./landing-anim.css";

// /landing 전용 레이아웃 — 랜딩 애니메이션 CSS만 로드.
export default function LandingLayout({ children }: { children: ReactNode }) {
  return children;
}
